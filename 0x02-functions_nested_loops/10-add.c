#include "main.h"

/**
 * add - adds to numbers
 * Description: 'calculate the addition of the two numbres a and b"
 * @a: the first number
 * @b: the second numbre
 * Return: the result of the addition
 */

int add(int a, int b)
{
	int res = a + b;

	return (res);
}
